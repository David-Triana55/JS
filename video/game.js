const canvas = document.querySelector("#game");
const game = canvas.getContext("2d");

window.addEventListener("load", startGame);

function startGame() {
    // game.fillReact(0,0,100,50)
    // game.clearReact(0,0,100,50)
    // game.filltext("hola",0,100,)
    // game.fillStyle = "black"
    // game.font = "Helvetica"
    // game.textAlign = "left"

    
}