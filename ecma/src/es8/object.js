// ? transforma los valores de la llave en un array => object.values()

let contries = {
    Mx : "Mexico" ,
    Co: "Colombia" ,
    Cl : "Chile",
    Pe : "Peru"
}

console.log(Object.values(contries));

// ? devuelve las llaves en un array => object.keys

console.log(Object.keys(contries));


// ? devuelve los objetos en array su llave y su valor => object.entries


console.log(Object.entries(contries));


