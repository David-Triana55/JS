const obj = {
nombre: "david",
edad: 18,
} 

const array = [1, 2, 3, 4]

array.push("perro hpta")
array.pop()
const map = array.map( nombre => nombre * 2)



console.log(obj.nombre + " "+ array + " " + map);